cd into this folder
then run uvicorn main:app --reload --port 8000

When to use each type of api get/post
    GET = asks the server for data
    POST = sends data to the server so it can process it, create something, or trigger an action
    